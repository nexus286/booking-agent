# Agents

Agent definitions will land here as the build progresses. Each agent (Scout, Outreach, Scheduler, Logistics, Research, CRM) gets its own module with system prompt, tool definitions, and any agent-specific configuration.
